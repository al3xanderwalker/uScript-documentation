# rotation

* Method: \[get\]
* Returns: \[number\]

> Example:

```css
command rotation(number){
  permission = "rotation";
  execute(){
    player.message("Your rotation is " + player.rotation);
  }
}
```

