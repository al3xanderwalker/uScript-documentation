# browserRequest

* Parameters: \[text\], \[url\]

> #### Example:

```css
command browserRequest(text){
  permission = "browserRequest";
  execute(){
    player.browserRequest(text, "website.url");
  }
}
```

